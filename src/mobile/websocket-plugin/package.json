{
  "name": "synergism-websocket",
  "version": "1.0.0",
  "description": "Native URLSessionWebSocketTask bridge so authenticated (cookie-based) WebSockets work inside the iOS WKWebView. See ../native-websocket.ts for the JS adapter.",
  "license": "MIT",
  "files": [
    "ios/Sources",
    "Package.swift"
  ],
  "capacitor": {
    "ios": {
      "src": "ios"
    }
  },
  "devDependencies": {
    "@capacitor/core": "^8.3.1",
    "@capacitor/ios": "^8.3.1"
  }
}
